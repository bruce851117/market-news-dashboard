name: Schedule Ping

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Print time
        run: |
          echo "Schedule ping triggered"
          echo "UTC time:"
          date -u
          echo "Local runner time:"
          date
